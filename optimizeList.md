# CodeAgenda 后续优化建议

> 基于当前项目结构与代码现状整理。当前版本已经具备完整的核心闭环：Flask 本地服务、SQLite 数据、热力图、题目明细、每日一题、导入导出、背景图、牛客同步脚本，以及 PyQt6 托盘控制。以下建议主要面向“更完整、更稳定、更规范、更容易维护”。

## 一、建议优先级

| 优先级 | 目标 | 建议先做的内容 |
| --- | --- | --- |
| P0 | 防止数据损坏和难以排错 | 自动化测试、数据库一致性、托盘异常退出检测、依赖和启动诊断 |
| P1 | 提升长期维护能力 | 分层重构、配置集中管理、数据库迁移、统一错误响应、前端请求封装 |
| P2 | 提升用户体验 | 搜索/筛选、统计图表、快捷键、主题、通知和更完善的同步状态 |
| P3 | 产品化和扩展能力 | 打包安装、系统自启动、插件化同步、数据同步与多设备迁移 |

## 二、P0：应该优先补齐的基础能力

### 1. 建立自动化测试体系

**现状**：仓库目前没有 `tests/`、测试配置或 CI。`app.py` 中已有大量日期校验、去重、导入导出和数据库同步规则，这些规则一旦改动容易回归。

**建议**：

- 使用 `pytest` 和 Flask `test_client()` 测试所有 API 的正常与异常分支。
- 优先覆盖：日期校验、负数/非法 count、重复题目、同链接不同题名、删除最后一道题、每日一题匹配、导入导出、背景图大小和扩展名校验。
- 为连续打卡、跨年热力图、未来日期不可编辑等边界建立纯函数测试。
- 测试使用临时目录和临时 SQLite，禁止读写真实 `data/`。
- 增加 `pytest-cov`，在 CI 中设定最低覆盖率门槛，例如第一阶段 70%，再逐步提高。

**收益**：后续重构 `app.py` 或前端时，能快速确认“功能没有变化”。这是当前最值得投入的工程化工作。

### 2. 引入 CI 和静态检查

**现状**：当前只能手工运行 `py_compile`，没有自动检查格式、类型、测试或依赖安装。

**建议**：增加 GitHub Actions：

1. 安装依赖；
2. 执行 `pytest`；
3. 执行 `ruff check` 或 `flake8`；
4. 执行 `python -m compileall`；
5. （可选）执行前端 ESLint/HTML 校验。

同时补充 `pyproject.toml`，集中配置 pytest、ruff、格式化工具和项目元数据。

### 3. 加强数据库一致性和备份保护

**现状**：记录汇总在 `records.db`，题目明细在 `details.db`。两库之间存在派生关系，代码通过同步函数维持一致；导入时如果中途失败，可能只完成部分写入。

**建议**：

- 中长期合并为一个 SQLite 数据库，或实现明确的跨库事务策略。
- 为 `records.date`、`submissions(date, problem_key)`、`daily_problems.date` 建立并检查索引。
- 将导入流程改成“解析 -> 校验全部数据 -> 开启事务 -> 批量写入 -> 提交”，失败则整体回滚。
- 导入前自动生成带时间戳的备份，而不是直接覆盖已有数据。
- 增加数据库 schema 版本表和迁移脚本，禁止以后依赖“启动时猜测旧表结构”。
- 提供“校验并修复汇总记录”命令，重新从题目明细重算 `records`。

### 4. 托盘和后端异常恢复

**现状**：托盘启动后端并读取 stdout；后端异常退出会被记录，但目前主要依赖用户观察日志。启动失败时，由于 `pythonw.exe` 隐藏窗口，用户可能只看到托盘没有出现。

**建议**：

- 启动后主动请求 `GET /api/summary`，确认服务真的可用，再显示“启动成功”。
- 后端进程退出时显示明确的托盘通知，并在日志中记录返回码和最后输出。
- 区分“端口被占用”“依赖缺失”“数据库初始化失败”“权限不足”等启动错误。
- 为 `CodeAgenda.vbs` 增加失败日志路径，避免隐藏窗口掩盖 Python 异常。
- 单实例锁最好改为命名 Mutex（或至少对锁文件做 PID 存活校验），并处理锁文件权限异常。
- 关闭时加入 `try/finally`，无论后端终止是否超时，都释放锁并隐藏托盘图标。

### 5. 依赖与启动环境诊断

**现状**：`requirements.txt` 已列出 Flask 和 PyQt6，但启动依赖 `pythonw.exe` 在 PATH 中；不同 Python 安装位置容易导致“命令行能运行、双击打不开”。

**建议**：

- 启动器优先使用项目配置的 Python 路径，或在启动前检测 `pythonw.exe`、Flask、PyQt6 并给出可见错误。
- 提供 `doctor.py`/“环境检查”命令：显示 Python 路径、版本、依赖版本、端口占用、数据库路径和写权限。
- 固定依赖版本范围，避免未来自动安装到不兼容的大版本。
- 增加 `requirements-dev.txt`，把 pytest、ruff、打包工具与运行时依赖分开。

## 三、P1：代码规范和可维护性

### 6. 拆分 `app.py`

**现状**：`app.py` 约 840 行，同时承担 Flask 初始化、数据库连接、迁移、业务规则、API、日志和启动入口。

**建议目录**：

```text
codeagenda/
  __init__.py        # create_app()
  config.py          # 配置与路径
  db.py              # 连接、初始化、迁移
  models.py          # 数据访问对象或查询函数
  services.py        # 汇总、去重、连续打卡等业务规则
  routes/            # records/submissions/settings/export 路由
  errors.py          # 统一错误处理
tests/
```

先做“移动代码、不改变行为”的拆分，再逐步改接口，避免一次性大重构。

### 7. 集中管理配置

**现状**：端口、项目路径、数据库路径、背景图大小、前端 URL 等配置散落在多个文件和环境变量中。

**建议**：使用 `config.py` 或 dataclass 集中管理：`HOST`、`PORT`、`DATA_DIR`、`WEB_URL`、`MAX_BG_SIZE`、日志级别、备份目录。启动脚本、Flask 和托盘共同读取同一套配置，避免换端口时手工修改多处。

### 8. 统一错误响应和输入校验

**现状**：大多数 API 已有校验，但部分路由的错误格式、字段长度限制和日志粒度仍不完全统一。

**建议**：

- 统一返回 `{error, code, details}` 结构，并保持 HTTP 状态码稳定。
- 使用集中校验函数或 schema（如 Pydantic/Marshmallow），避免每个路由重复判断。
- 明确字符串最大长度、URL 允许的 scheme/host、日期是否允许未来值。
- 统一处理 JSON 解析失败、SQLite 异常、未知路由和未捕获异常。
- 不把内部路径、SQL 错误或堆栈直接返回给浏览器；详细信息只写日志。

### 9. 日志进一步规范化

**现状**：日志已经有时间戳、级别、请求耗时和托盘日志，完成度不错；但日志内容仍是自由文本，前后端无法方便地筛选和统计。

**建议**：

- 使用结构化字段：事件名、日期、请求路径、状态码、耗时、异常类型。
- 给每次请求增加 request id，前端报错时可直接对应日志。
- 日志窗口支持级别筛选、关键字搜索、暂停自动滚动、复制选中内容。
- 导出日志采用 UTF-8 with BOM 或明确编码提示，方便 Windows 文本工具打开。
- 对密码、完整账号、文件路径等敏感信息做统一脱敏。

### 10. 前端 JavaScript 分层

**现状**：`static/app.js` 约 680 行，包含请求、状态、热力图渲染、模态框、背景设置、导入导出和轮询。

**建议**：拆成 `api.js`、`state.js`、`heatmap.js`、`modal.js`、`settings.js`，或至少按区域组织模块。集中封装 `fetch`：统一超时、错误解析、重试和请求状态。将 DOM 更新从业务请求中分离，减少重复刷新和竞态。

### 11. 统一时间处理

**现状**：日期使用 `date.today()`，时间字符串使用本机本地时间；用户脚本和后端也分别计算“今天”。

**建议**：明确项目时区（例如 `Asia/Shanghai`），统一提供日期/时间函数。所有持久化时间建议使用 ISO 8601，必要时带时区；跨午夜、夏令时和导入其他机器时更可靠。

## 四、P1/P2：功能完整度和用户体验

### 12. 记录查询和分析能力

- 增加按日期范围、题目名称、链接、是否每日一题的搜索。
- 增加月度/季度统计：总题数、活跃天数、平均每天、最长连续记录。
- 增加题目标签、分类、难度和来源字段，为后续分析预留结构。
- 支持点击统计卡进入明细，而不是只能从热力图逐日查看。
- 提供 CSV 导出，方便 Excel 或数据分析工具使用。

### 13. 数据编辑体验

- 支持批量删除、批量导入校验报告和冲突预览。
- 编辑题目名称/链接时保留历史变更，避免误改后无法追踪。
- 导入前显示新增、更新、重复、冲突数量。
- 为删除操作增加撤销窗口或最近删除列表。
- 处理网络断开、后端重启时的前端离线状态和重试按钮。

### 14. 牛客同步脚本的可维护性

**现状**：用户脚本对页面文本和 DOM 结构有较强依赖，包含调试函数和请求去重逻辑。

**建议**：

- 将 DOM 选择器和成功文案集中到配置对象，便于牛客改版后快速调整。
- 为 `isDailyComplete`、AC 信号识别、题目 URL 提取增加本地 mock 测试样例。
- 增加同步状态面板：最近一次扫描时间、最近一次成功、失败原因、已忽略重复数。
- 为请求增加幂等 key 或客户端事件 id，进一步降低重复提交风险。
- 清晰区分“页面扫描失败”“接口不可达”“后端拒绝”“重复题目”。

### 15. 可访问性和国际化

- 给热力图方块提供键盘访问和清晰的 `aria-label`。
- 检查颜色对比度，不要只用颜色表达“已完成/未完成”。
- 统一按钮、弹窗和错误提示的焦点管理。
- 将中文文案集中管理，为英文界面或其他语言预留入口。

## 五、P2/P3：产品化方向

### 16. Windows 打包和安装

**现状**：当前依赖本机 Python、PyQt6 和 PATH，使用 VBS/BAT 启动，适合开发和个人使用，但不适合分发。

**建议**：

- 使用 PyInstaller/Nuitka 打包为 `CodeAgenda.exe`，带上 PyQt6 平台插件和 `favicon.svg`。
- 增加版本号、应用图标、卸载入口和升级说明。
- 安装时选择数据目录；升级时保留数据库和背景图。
- 提供便携版 ZIP，适合不想安装的用户。
- 在发布前验证 Windows 10/11、无 Python 环境、中文路径和无管理员权限场景。

### 17. 系统集成

- 支持开机自启动（需提供明确设置项）。
- 托盘菜单显示后端运行状态、当前端口和数据目录。
- 支持“重启后端”而不退出托盘。
- 增加端口配置界面和“打开数据目录/日志目录”操作。
- 当后端不可用时，托盘状态图标和通知应明确区分运行、警告、停止。

### 18. 数据同步和扩展

在本地稳定后再考虑：

- WebDAV、Git 私有仓库或用户自选目录备份。
- 多设备合并时的冲突策略和事件日志。
- 同步适配器接口，让牛客脚本、浏览器扩展或其他题库成为独立模块。
- 将题目、学习计划、标签、目标等扩展为可选模块，而不是继续把所有逻辑堆进单文件。

## 六、安全和隐私专项

### 19. 账号密码保护

**现状**：README 已明确说明牛客密码以明文保存在本地 SQLite。对于个人本机可以接受，但这是项目最明显的安全风险。

**建议**：

- 优先改为 Windows Credential Manager（或系统钥匙串）存储密码。
- 数据库只保存账号和凭据引用，不保存明文密码。
- 保存密码时在 UI 中明确提示用途和存储位置。
- 日志、导出备份、异常信息中继续禁止出现密码。
- 提供清除凭据和注销同步功能。

### 20. 本地服务边界

- 继续绑定 `127.0.0.1`，不要默认监听 `0.0.0.0`。
- 对所有写接口增加 CSRF/本地 token 防护，避免其他本机网页诱导请求。
- 限制上传文件的 MIME、扩展名、大小和文件名；使用随机文件名并防止路径穿越。
- 对导入 JSON 设置最大体积和记录数量上限，避免意外耗尽资源。
- 明确“本地服务不等于绝对安全”，在 README 中给出威胁边界。

## 七、推荐实施路线

### 阶段 1：稳定性（1 个迭代）

- 建立 pytest 基础和临时数据库夹具。
- 覆盖 API、去重、导入导出、连续打卡和异常输入。
- 增加 CI、静态检查和启动环境诊断。
- 完善托盘后端健康检查、异常通知和重启操作。

### 阶段 2：规范化（1~2 个迭代）

- 集中配置端口、路径、日志和数据目录。
- 增加 schema 版本与数据库迁移。
- 拆分 `app.py` 的路由、数据访问和业务服务。
- 统一 API 错误格式和前端请求封装。

### 阶段 3：体验提升（持续迭代）

- 增加搜索、统计分析、批量操作和导入预览。
- 优化同步脚本的可观测性和兼容性。
- 完善日志筛选、复制、导出和运行状态展示。
- 补齐键盘访问、响应式布局和国际化基础。

### 阶段 4：产品化（需求成熟后）

- PyInstaller/Nuitka 打包 EXE。
- 安装、升级、便携版和开机自启动。
- 凭据安全存储与可选云/私有备份。

## 八、可以立刻执行的小改进

1. 增加 `pytest` 的第一组 API 测试，先保护现有行为。
2. 增加“重启后端”托盘菜单项，并在后端退出时弹出通知。
3. 把 `PORT`、`WEB_URL`、数据目录和背景限制集中到一个配置模块。
4. 为导入操作增加自动备份和失败回滚。
5. 为日志窗口增加搜索、复制全部和级别筛选。
6. 将明文密码迁移到 Windows Credential Manager。
7. 清理并统一 README、`.gitignore`、启动脚本和依赖版本说明，避免文档与实际行为再次漂移。
